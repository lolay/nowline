<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 1400 720" width="1400" height="720" font-family="-apple-system, 'SF Pro Display', 'Segoe UI', Helvetica, Arial, sans-serif">
  <!--
    Sample - Platform 2026 (full-featured example)

    Demonstrates the full spectrum of rendering decisions:
      - Roadmap header with company logo + Nowline attribution mark
      - Timeline scale (weekly ticks), 16 columns at 70px each
      - Now-line running full height with pill label at top
      - Anchors (kickoff, code-freeze, ga-date) - separate header row (dashed vertical)
      - Milestones (Beta, v1 GA, GA launch) - solid black verticals through the chart
          * Beta (after:auth-refactor)         - line at x=470 (rightmost predecessor)
          * v1 GA (after:[auth-refactor,audit-log]) - line at x=618 (audit-log end);
            dotted dark connector arrow from auth-refactor end -> line
          * GA launch (date:Mar 16) - line at x=1030 (date-driven, no connectors)
          * Red milestone state only applies when an `after:` predecessor ends to
            the right of the line; none do here, so all three render in dark ink.
      - Two swimlanes (Platform + Mobile), each with lane tabs and owner badges
      - Parallel region with a styled group (Audit Track) + a bare track (SSO)
      - Label chiclets (enterprise, security, low-confidence)
      - Status dot in upper-RIGHT corner of each item bar
      - Footnote superscript in upper-right, immediately LEFT of the status dot
      - Progress fill along bottom of item bars
      - Link icon (Linear) on auth-refactor
      - Footnote block at bottom of canvas
      - Dependency arrow (auth-refactor -> audit-log)

    Timeline math (1w = 70px, start Jan 05):
      x=260 Jan05 (kickoff)       x=680 Feb 16
      x=330 Jan12                 x=750 Feb 23
      x=400 Jan19                 x=820 Mar 02 (code-freeze anchor)
      x=470 Jan26                 x=890 Mar 09
      x=540 Feb02                 x=960 Mar 16 (ga-date anchor / GA launch milestone)
      x=610 Feb 09                x=1030 Mar 23
  -->

  <defs>
    <filter id="nl-shadow" x="-8%" y="-8%" width="116%" height="140%">
      <feDropShadow dx="0" dy="1" stdDeviation="1.5" flood-color="#0f172a" flood-opacity="0.14"/>
    </filter>
    <marker id="nl-arrow" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto">
      <path d="M 0 0 L 10 5 L 0 10 z" fill="#475569"/>
    </marker>
    <marker id="nl-arrow-light" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="6" markerHeight="6" orient="auto">
      <path d="M 0 0 L 10 5 L 0 10 z" fill="#94a3b8"/>
    </marker>
    <marker id="nl-arrow-dark" viewBox="0 0 10 10" refX="9" refY="5" markerWidth="7" markerHeight="7" orient="auto">
      <path d="M 0 0 L 10 5 L 0 10 z" fill="#0f172a"/>
    </marker>
  </defs>

  <!-- Canvas -->
  <rect width="1400" height="720" fill="#f8fafc"/>

  <!-- =========================================================
       Roadmap header - company logo + title + attribution mark
       ========================================================= -->
  <g transform="translate(24,20)">
    <rect width="220" height="76" rx="6" fill="#ffffff" stroke="#e2e8f0" filter="url(#nl-shadow)"/>
    <g transform="translate(14,14)">
      <rect width="48" height="48" rx="8" fill="#0f172a"/>
      <text x="24" y="32" font-size="24" font-weight="700" fill="#ffffff" text-anchor="middle">A</text>
    </g>
    <text x="74" y="36" font-size="17" font-weight="700" fill="#0f172a">Platform 2026</text>
    <text x="74" y="54" font-size="11" fill="#64748b">Acme Engineering</text>
  </g>

  <!-- =========================================================
       Timeline header - merged marker row + week labels
         Row M (y=20..46): Anchors AND Milestones share one row,
                           diamond centers on the same baseline
                           (y=33). When two markers at the same
                           x-column would collide, the anchor is
                           bumped up into the slim band (y=4..20)
                           above so the anchor sits ABOVE the
                           milestone. Otherwise the anchor sits
                           in-row with its label to the right.
         Row L (y=46..70): Week tick labels.
       ========================================================= -->

  <!-- Merged anchor + milestone row -->
  <g>
    <rect x="258" y="20" width="1118" height="26" fill="#ffffff" stroke="#e2e8f0" rx="4"/>

    <!-- kickoff anchor (x=260) - in-row, label right -->
    <g transform="translate(260,33)">
      <polygon points="0,-6 6,0 0,6 -6,0" fill="#ffffff" stroke="#334155" stroke-width="1.25"/>
    </g>
    <text x="272" y="37" font-size="10" fill="#334155">kickoff</text>

    <!-- Beta milestone (x=470) - in-row, label right -->
    <g transform="translate(470,33)">
      <polygon points="0,-6 6,0 0,6 -6,0" fill="#0f172a"/>
    </g>
    <text x="482" y="37" font-size="10" font-weight="600" fill="#0f172a">Beta</text>

    <!-- v1 GA milestone (x=618) - in-row, label right -->
    <g transform="translate(618,33)">
      <polygon points="0,-6 6,0 0,6 -6,0" fill="#0f172a"/>
    </g>
    <text x="630" y="37" font-size="10" font-weight="600" fill="#0f172a">v1 GA</text>

    <!-- Code Freeze anchor (x=820) - in-row, label right -->
    <g transform="translate(820,33)">
      <polygon points="0,-6 6,0 0,6 -6,0" fill="#ffffff" stroke="#334155" stroke-width="1.25"/>
    </g>
    <text x="828" y="37" font-size="10" fill="#334155">Code Freeze</text>

    <!-- ================= Collision at x=960 =================
         GA Date (anchor) and GA launch (milestone) share x=960.
         The anchor is pushed UP into the collision band (y=4..20)
         so it sits ABOVE the milestone, which stays in-row at y=33.
         ======================================================= -->
    <!-- GA Date anchor (pushed above the row) -->
    <g transform="translate(960,12)">
      <polygon points="0,-6 6,0 0,6 -6,0" fill="#ffffff" stroke="#334155" stroke-width="1.25"/>
    </g>
    <text x="972" y="16" font-size="10" fill="#334155">GA Date</text>
    <!-- GA launch milestone (stays in-row) -->
    <g transform="translate(960,33)">
      <polygon points="0,-6 6,0 0,6 -6,0" fill="#0f172a"/>
    </g>
    <text x="972" y="37" font-size="10" font-weight="600" fill="#0f172a">GA launch</text>
  </g>

  <!-- Week tick labels (row lifted to y=46..70, chart top pushed up 26px) -->
  <g transform="translate(258,46)">
    <rect width="1118" height="24" fill="#ffffff" stroke="#e2e8f0" rx="4"/>
    <g font-size="10" fill="#64748b" text-anchor="middle">
      <text x="2"   y="15">Jan 05</text>
      <text x="72"  y="15">Jan 12</text>
      <text x="142" y="15">Jan 19</text>
      <text x="212" y="15">Jan 26</text>
      <text x="282" y="15">Feb 02</text>
      <text x="352" y="15">Feb 09</text>
      <text x="422" y="15">Feb 16</text>
      <text x="492" y="15">Feb 23</text>
      <text x="562" y="15">Mar 02</text>
      <text x="632" y="15">Mar 09</text>
      <text x="702" y="15">Mar 16</text>
      <text x="772" y="15">Mar 23</text>
      <text x="842" y="15">Mar 30</text>
      <text x="912" y="15">Apr 06</text>
      <text x="982" y="15">Apr 13</text>
      <text x="1052" y="15">Apr 20</text>
    </g>
  </g>

  <!-- Weekly grid -->
  <g stroke="#e2e8f0" stroke-width="1" stroke-dasharray="2 3">
    <line x1="330"  y1="96" x2="330"  y2="580"/>
    <line x1="400"  y1="96" x2="400"  y2="580"/>
    <line x1="540"  y1="96" x2="540"  y2="580"/>
    <line x1="610"  y1="96" x2="610"  y2="580"/>
    <line x1="680"  y1="96" x2="680"  y2="580"/>
    <line x1="750"  y1="96" x2="750"  y2="580"/>
    <line x1="890"  y1="96" x2="890"  y2="580"/>
    <line x1="1030" y1="96" x2="1030" y2="580"/>
    <line x1="1100" y1="96" x2="1100" y2="580"/>
    <line x1="1170" y1="96" x2="1170" y2="580"/>
    <line x1="1240" y1="96" x2="1240" y2="580"/>
    <line x1="1310" y1="96" x2="1310" y2="580"/>
  </g>

  <!-- =========================================================
       Swimlane 1 - Platform (y=110..430, 320 tall)
       ========================================================= -->
  <g>
    <rect x="24" y="110" width="1352" height="320" fill="#ffffff" stroke="#e2e8f0"/>
    <!-- Lane tab (capacity-risk footnote superscript sits in the upper-right
         corner *inside* the tab, right-aligned). -->
    <g>
      <rect x="34" y="120" width="210" height="24" rx="4" fill="#eef2ff" stroke="#c7d2fe"/>
      <text x="46" y="136" font-size="12" font-weight="600" fill="#3730a3">Platform</text>
      <text x="120" y="136" font-size="10" fill="#6366f1">owner: Platform Team</text>
      <text x="238" y="132" font-size="10" font-weight="700" fill="#dc2626" text-anchor="end">&#178;</text>
    </g>

    <!-- Row 1: auth-refactor -> parallel(audit-track + sso) -> platform-qa -->

    <!-- auth-refactor: duration 3w (logical x=260..470). With 6px inset on each
         side, visual bar sits at absolute x=266..464 (width 198). 12px gutter to
         the next item's visual left edge. -->
    <g transform="translate(266,160)">
      <rect width="198" height="60" rx="4" fill="#ecfdf5" stroke="#10b981" filter="url(#nl-shadow)"/>
      <text x="12" y="20" font-size="13" font-weight="600" fill="#064e3b">Auth refactor</text>
      <text x="12" y="36" font-size="10" fill="#047857">Sam Chen</text>
      <!-- enterprise label chiclet (inside content zone, 3px padding to progress strip below) -->
      <g transform="translate(12,40)">
        <rect width="58" height="13" rx="6" fill="#1e40af"/>
        <text x="29" y="10" font-size="8" font-weight="600" fill="#ffffff" text-anchor="middle">enterprise</text>
      </g>
      <!-- link icon (Linear-tinted tile, external-link glyph) - bottom right -->
      <g transform="translate(174,38)">
        <rect width="14" height="14" rx="2" fill="#5e6ad2"/>
        <path d="M4 10 L10 4 M6 4 H10 V8" stroke="#ffffff" fill="none" stroke-width="1.1" stroke-linecap="round" stroke-linejoin="round"/>
      </g>
      <!-- status dot upper-right (green = done) -->
      <circle cx="186" cy="12" r="5" fill="#10b981"/>
      <!-- progress fill (100%) - bottom 4px strip, separated from label chiclet above -->
      <rect x="0" y="56" width="198" height="4" fill="#10b981" opacity="0.55"/>
    </g>

    <!-- =========================================================
         Parallel region starts at logical x=470 (end of auth-refactor's
         logical column). Two tracks:
           Track 1: Audit Track (styled group)
           Track 2: SSO (bare item)
         Both tracks also apply the 6px left inset so nested items sit
         inside the parallel's left edge with the same gutter.
         Group logical extent: [470, 626], visual: [476, 620] (w=144).
         Parallel join at logical x=630. After join: platform-qa.
         ========================================================= -->

    <!-- Audit Track styled group (orange), w=144 visual (156 logical - 12 gutter),
         y=232..356. Nested items inside live in a fresh coord system where
         inner padding = 8 (matches the group tab's left inset). -->
    <g transform="translate(476,232)">
      <!-- group rectangle -->
      <rect width="144" height="124" rx="6" fill="#fff7ed" stroke="#fb923c" filter="url(#nl-shadow)"/>
      <!-- group label chiclet top-left -->
      <g transform="translate(8,-11)">
        <rect width="76" height="18" rx="3" fill="#fb923c"/>
        <text x="38" y="12" font-size="9" font-weight="600" fill="#ffffff" text-anchor="middle">Audit Track</text>
      </g>
      <!-- group-level security label chiclet (right of the group tab, no overlap) -->
      <g transform="translate(90,-11)">
        <rect width="54" height="18" rx="9" fill="#1e40af"/>
        <text x="27" y="12" font-size="8" font-weight="600" fill="#ffffff" text-anchor="middle">security</text>
      </g>

      <!-- Nested parallel indicator (subtle dashed rails at group's inner
           left/right, hugging the nested items). -->
      <g stroke="#fdba74" fill="none" stroke-width="1" stroke-dasharray="3 3">
        <path d="M 8 12 V 112 M 136 12 V 112"/>
      </g>

      <!-- audit-log: logical 2w=140, visual 128 after symmetric gutter,
           in-progress, 30% remaining, footnote 1. -->
      <!-- row 1 of nested parallel, inner x=8, y=12 -->
      <g transform="translate(8,12)">
        <rect width="128" height="46" rx="4" fill="#eff6ff" stroke="#3b82f6" filter="url(#nl-shadow)"/>
        <text x="10" y="18" font-size="12" font-weight="600" fill="#1e3a8a">Audit log v2</text>
        <text x="10" y="32" font-size="9" fill="#1d4ed8">30% remaining</text>
        <!-- progress (70% done) -->
        <rect x="0" y="42" width="90" height="4" fill="#3b82f6" opacity="0.6"/>
        <!-- footnote superscript upper-right, LEFT of status dot -->
        <text x="105" y="12" font-size="9" font-weight="700" fill="#dc2626">&#185;</text>
        <!-- status dot upper-right -->
        <circle cx="118" cy="10" r="5" fill="#3b82f6"/>
        <!-- generic external link icon - bottom right -->
        <g transform="translate(110,28)">
          <rect width="12" height="12" rx="2" fill="#0f172a"/>
          <path d="M4 8 L8 4 M5 4 H8 V7" stroke="#ffffff" fill="none" stroke-width="1" stroke-linecap="round" stroke-linejoin="round"/>
        </g>
      </g>

      <!-- audit-ui: logical 1w=70, visual 58 after symmetric gutter.
           Narrow item - title wraps over two lines to preserve the status-dot area. -->
      <g transform="translate(8,66)">
        <rect width="58" height="46" rx="4" fill="#f8fafc" stroke="#94a3b8" filter="url(#nl-shadow)"/>
        <text x="10" y="22" font-size="11" font-weight="600" fill="#1e293b">Audit</text>
        <text x="10" y="34" font-size="11" font-weight="600" fill="#1e293b">UI</text>
        <circle cx="46" cy="10" r="5" fill="#94a3b8"/>
      </g>
    </g>

    <!-- SSO bare parallel track (sibling of Audit Track group, under the group).
         Logical 1w=70, visual 58 after symmetric gutter. -->
    <g transform="translate(476,372)">
      <rect width="58" height="46" rx="4" fill="#f8fafc" stroke="#94a3b8" filter="url(#nl-shadow)"/>
      <text x="12" y="18" font-size="12" font-weight="600" fill="#1e293b">SSO</text>
      <text x="12" y="32" font-size="9" fill="#64748b">1w</text>
      <circle cx="46" cy="10" r="5" fill="#94a3b8"/>
    </g>

    <!-- No outer parallel rails: the outer parallel block is `bracket:none`
         (no `style:` applied), so per the rendering spec it renders as purely
         structural — items stack on tracks with no visual connector. -->

    <!-- platform-qa: next sibling after the parallel block. It sits at the
         parallel's logical end (x=630) with the 6px visual inset -> x=636.
         No dependency arrows are drawn: platform-qa has no `after:`, and the
         outer parallel is `bracket:none` (no fork/join indication). Its x
         position alone conveys that it follows the parallel. -->
    <g transform="translate(636,282)">
      <rect width="58" height="60" rx="4" fill="#f8fafc" stroke="#94a3b8" filter="url(#nl-shadow)"/>
      <text x="12" y="18" font-size="12" font-weight="600" fill="#1e293b">QA</text>
      <text x="12" y="32" font-size="10" fill="#64748b">3d</text>
      <text x="12" y="48" font-size="9" font-weight="600" fill="#7c3aed">Handoff</text>
      <circle cx="46" cy="10" r="5" fill="#7c3aed"/>
    </g>
  </g>

  <!-- =========================================================
       Swimlane 2 - Mobile (y=430..580)
       ========================================================= -->
  <g>
    <rect x="24" y="430" width="1352" height="150" fill="#fafafa" stroke="#e2e8f0"/>
    <line x1="24" y1="430" x2="1376" y2="430" stroke="#cbd5e1"/>

    <g>
      <rect x="34" y="440" width="200" height="24" rx="4" fill="#fef3c7" stroke="#fcd34d"/>
      <text x="46" y="456" font-size="12" font-weight="600" fill="#92400e">Mobile</text>
      <text x="110" y="456" font-size="10" fill="#b45309">owner: Mobile Team</text>
      <text x="228" y="452" font-size="10" font-weight="700" fill="#dc2626" text-anchor="end">&#178;</text>
    </g>

    <!-- Offline mode: at-risk, logical 3w=210, visual 198 after symmetric gutter -->
    <g transform="translate(266,480)">
      <rect width="198" height="60" rx="4" fill="#fefce8" stroke="#eab308" filter="url(#nl-shadow)"/>
      <text x="12" y="20" font-size="13" font-weight="600" fill="#713f12">Offline mode</text>
      <text x="12" y="36" font-size="10" fill="#a16207">Jennifer Wu - 60% remaining</text>
      <!-- low-confidence label chiclet (3px padding to progress strip below) -->
      <g transform="translate(12,40)">
        <rect width="94" height="13" rx="6" fill="#ffffff" stroke="#fb923c" stroke-dasharray="3 2"/>
        <text x="47" y="10" font-size="8" font-weight="600" fill="#c2410c" text-anchor="middle">low-confidence</text>
      </g>
      <!-- status dot upper-right -->
      <circle cx="186" cy="12" r="5" fill="#eab308"/>
      <!-- progress (40% done) - bottom 4px strip, separated from label chiclet above -->
      <rect x="0" y="56" width="79" height="4" fill="#eab308" opacity="0.65"/>
    </g>

    <!-- Push v2: planned, logical 2w=140, visual 128 after symmetric gutter, starts after Offline mode -->
    <g transform="translate(476,480)">
      <rect width="128" height="60" rx="4" fill="#f8fafc" stroke="#94a3b8" filter="url(#nl-shadow)"/>
      <text x="12" y="20" font-size="13" font-weight="600" fill="#1e293b">Push v2</text>
      <text x="12" y="36" font-size="10" fill="#64748b">2w</text>
      <circle cx="116" cy="12" r="5" fill="#94a3b8"/>
    </g>
  </g>

  <!-- =========================================================
       Dependency arrow: auth-refactor (Platform) -> audit-log start.
       Uses VISUAL bar edges so the arrow emerges from and terminates
       on the actual bars, not in the inter-column gutter.
       auth-refactor visual right = 464. audit-log visual left (absolute) = 484.
       ========================================================= -->
  <g stroke="#475569" fill="none" stroke-width="1.1" marker-end="url(#nl-arrow)" stroke-linejoin="round">
    <path d="M 464 190 H 474 V 265 H 482"/>
  </g>

  <!-- =========================================================
       Milestone predecessor connector arrow (dark dotted).
       v1 GA milestone line sits at x=618 (audit-log's logical end).
       auth-refactor visual right (x=464) -> route rightward to the line.
       ========================================================= -->
  <g stroke="#0f172a" fill="none" stroke-width="1.1" stroke-dasharray="3 3" stroke-linecap="round" marker-end="url(#nl-arrow-dark)">
    <path d="M 464 190 H 612"/>
  </g>

  <!-- =========================================================
       Anchor cut lines (dashed, neutral) - drawn late so they
       render OVER the swimlane fills, like the now-line does.
       Items are still drawn earlier so they visually "cut" the
       line rather than the line cutting through items.
         kickoff @ x=260 (dashed), Code Freeze @ x=820 (dashed).
         GA Date anchor @ x=960 is visually superseded by the GA
         launch milestone line drawn below.
       ========================================================= -->
  <g stroke="#64748b" stroke-width="1" stroke-dasharray="1 3" fill="none">
    <!-- kickoff anchor: emerges from diamond bottom tip (y=39) -->
    <line x1="260" y1="39" x2="260" y2="580"/>
    <!-- Code Freeze anchor -->
    <line x1="820" y1="39" x2="820" y2="580"/>
  </g>

  <!-- =========================================================
       Milestone cut lines - drawn late so they render OVER the
       swimlane fills.
         Prominent dashed ink verticals: strong hue (indigo-ink),
         thicker stroke, visible dash pattern. Reads as "a hard
         gate that cuts the timeline."
         Beta at x=470; v1 GA at x=618; GA launch at x=960.
         Lines emerge from the bottom tip of the milestone diamond
         (y=39) and run down through the swimlanes.
         All three render dark because no `after:` predecessor ends
         to the right of its milestone line.
       ========================================================= -->
  <g stroke="#1e1b4b" stroke-width="2" stroke-dasharray="6 4" stroke-linecap="round" fill="none">
    <line x1="470" y1="39"  x2="470" y2="580"/>
    <line x1="618" y1="39"  x2="618" y2="580"/>
    <line x1="960" y1="39"  x2="960" y2="580"/>
  </g>

  <!-- =========================================================
       Now-line - Feb 09 (x=610)
       ========================================================= -->
  <g>
    <line x1="610" y1="20" x2="610" y2="580" stroke="#e53e3e" stroke-width="2.25"/>
    <g transform="translate(610,14)">
      <rect x="-20" y="-14" width="40" height="18" rx="9" fill="#e53e3e"/>
      <text x="0" y="-1" font-size="10" font-weight="700" fill="#ffffff" text-anchor="middle">now</text>
    </g>
  </g>

  <!-- =========================================================
       Footnote area at the bottom of the canvas
       ========================================================= -->
  <g>
    <rect x="24" y="604" width="1352" height="92" rx="6" fill="#ffffff" stroke="#e2e8f0"/>
    <text x="40" y="628" font-size="12" font-weight="700" fill="#0f172a">Footnotes</text>
    <g transform="translate(40,642)">
      <text x="0" y="14" font-size="10" font-weight="700" fill="#dc2626">1</text>
      <text x="16" y="14" font-size="11" font-weight="600" fill="#0f172a">Vendor dependency</text>
      <text x="134" y="14" font-size="11" fill="#64748b">- Blocked until vendor contract is signed. Expected March resolution.</text>
    </g>
    <g transform="translate(40,664)">
      <text x="0" y="14" font-size="10" font-weight="700" fill="#dc2626">2</text>
      <text x="16" y="14" font-size="11" font-weight="600" fill="#0f172a">Team capacity risk</text>
      <text x="134" y="14" font-size="11" fill="#64748b">- Mobile team is down to 2 engineers through Q2.</text>
    </g>
    <!-- Nowline attribution mark - bottom-right of the footnote panel (since
         footnotes exist in this sample). Panel bounds: x=[24,1376], y=[604,696].
         Mark anchored 12px in from the panel's bottom-right corner. Rendered
         last so it draws on top of panel content and never extends below. -->
    <g transform="translate(1331,673) scale(0.22)">
      <text x="0" y="40" font-size="40" font-weight="700" fill="#94a3b8">now</text>
      <rect x="92" y="12" width="5" height="40" fill="#e53e3e"/>
      <text x="100" y="40" font-size="40" font-weight="400" fill="#94a3b8">ine</text>
    </g>
  </g>
</svg>
