{
  "private": true,
  "name": "nowline-monorepo",
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "pnpm -r run test",
    "lint": "pnpm -r run lint",
    "langium:generate": "pnpm --filter @nowline/core run langium:generate"
  },
  "devDependencies": {
    "typescript": "~5.7.3"
  }
}
