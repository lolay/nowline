{
  "name": "layout-v2-prototype",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Standalone .scratch prototype validating a layout v2 architecture for @nowline/layout (scales + measure/place tree + view presets + non-continuous calendar). See findings.md.",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "run": "tsx bin/run.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "d3-scale": "^4.0.2",
    "d3-time": "^3.1.0"
  },
  "devDependencies": {
    "@types/d3-scale": "^4.0.8",
    "@types/d3-time": "^3.0.3",
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "~5.7.3",
    "vitest": "^3.1.0"
  }
}
